<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <title><%= content_for(:title) || "Transportation Tracker" %></title>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
          <div class="container-fluid">
            <a class="navbar-brand" href="/">Transportation Tracker</a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
              <ul class="navbar-nav me-auto">
                <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
                <li class="nav-item"><a class="nav-link" href="/about">About</a></li>
                <li class="nav-item"><a class="nav-link" href="/cities">Cities</a></li>
                <li class="nav-item"><a class="nav-link" href="/bus_routes">Bus Routes</a></li>
                <li class="nav-item"><a class="nav-link" href="/stops">Bus Stops</a></li>
              </ul>

              <!-- Search Form (Flexbox for alignment) -->
              <form action="/search" method="get" class="d-flex">
                <input class="form-control me-2" type="text" id="query" name="query" placeholder="Search..." required>
                
                <select class="form-select me-2" id="category" name="category">
                  <option value="all">All</option>
                  <option value="cities">Cities</option>
                  <option value="bus_routes">Bus Routes</option>
                  <option value="stops">Bus Stops</option>
                </select>
                
                <select class="form-select me-2" id="sub_category" name="sub_category">
                  <option value="">None</option>
                  <option value="routes_in_city">Routes in a City</option>
                  <option value="stops_in_route">Stops in a Bus Route</option>
                </select>

                <button class="btn btn-primary" type="submit">Search</button>
              </form>
            </div>
          </div>
        </nav>
      </header>

    <!-- Main Content -->
    <div class="container mt-4">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light text-center py-3 mt-4">
      <p>&copy; <%= Time.now.year %> Transportation Tracker</p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
